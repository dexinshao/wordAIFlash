# 【Code With SOLO】用 SOLO 搭建一个 AI 驱动的英语单词学习应用

## 1. 摘要

用 TRAE SOLO 从零搭建了一个带 AI 智能生成词库功能的英语单词学习应用 WordFlash。整个过程中，我只负责提需求和做决策，SOLO 完成了所有代码编写、数据库设计、API 对接和前端开发。AI 生成词库、智能匹配、已掌握单词过滤等功能，全部在一个对话中迭代完成。

## 2. 背景

我是一名开发者，想做一个英语单词学习工具。核心需求是：用户告诉 AI 自己想学什么主题（比如"天气"、"商务"），AI 自动生成相关单词列表，用户可以预览、删减，然后直接开始学习。

技术栈选择：React + tRPC + SQLite + 阿里云通义千问 API。

## 3. 实践过程

### 3.1 任务拆解

我没有一次性把所有需求丢给 SOLO，而是按功能模块逐步迭代：

**第一轮：基础功能**
- 首页展示内置词库（四六级、托福、雅思等）
- AI 根据主题生成单词列表
- 预览生成的单词，支持删除

**第二轮：智能过滤**
- 删除的单词标记为"已掌握"，下次 AI 生成时自动跳过
- 不在任何词库中的已掌握单词，自动归入"已掌握"词库

**第三轮：词库管理**
- AI 生成时排除指定词库的已有单词
- 生成后可选择合并到已有词库或创建新词库
- 首页支持删除词库，删除时可选择合并单词到其他词库

**第四轮：AI 智能匹配**
- 用 AI 判断主题与哪些词库相关，自动推荐
- 排除词库选择器默认折叠，展开时 AI 实时匹配

### 3.2 关键 Prompt 示例

> "AI 生成词库的时候，支持选择某个词库，生成时不仅要过滤掉已掌握的单词，还要过滤掉选择的该词库的所有单词"

> "如果删除的词不在任何一个词库里，把它添加到一个专门的【已掌握】词库，下次生成的时候过滤掉所有词库里已掌握的单词"

> "用 AI 来判断主题和词库是否匹配，不要用硬编码的中文映射表"

### 3.3 踩过的坑

**坑 1：API Key 明文存储**

一开始 SOLO 在 .env 里写了明文 API Key。我指出"不是已经改成密文存储了吗"，SOLO 立刻修正，改为 AES-256-GCM 加密存储。

**坑 2：AI 返回格式不兼容**

用 AI 匹配词库时，AI 返回了 `[{"id":9}]` 和 `["9"]` 等多种格式，而代码只处理了 `[9]`。调试发现后改为兼容三种格式。

**坑 3：复用了错误的 AI 调用方法**

词库匹配功能复用了"生成单词"的 AI 方法（system prompt 是"英语词汇专家"），导致 AI 返回格式混乱。最终拆分为独立的 `callAILite` 方法。

**坑 4：流程理解反了**

我要求"先选排除词库再生成"，SOLO 理解成了"先生成再匹配"。通过对话纠正后，改为折叠按钮 + 点击展开时调用 AI 的交互方式。

## 4. 成果展示

![首页](screenshot-home.png)

*首页展示内置词库和 AI 智能生成入口*

![AI 对话框](screenshot-dialog.png)

*输入主题，快速选择，一键生成*

![智能排除](screenshot-exclude.png)

*展开排除词库，AI 自动匹配最相关的词库*

### 核心功能清单

- 内置 8 个考试词库（四六级、托福、雅思、专八、BEC、高中、高频 10000）
- AI 根据任意主题生成 15-30 个英语单词
- 单词自动标注来源词库（【四级】【高中】等标签）
- 已掌握单词自动过滤，支持跨词库标记
- 词库智能匹配：AI 判断主题相关性并推荐
- 词库管理：创建、合并、删除，删除时可选合并单词
- 阿里云通义千问 qwen-turbo，每月 100 万 Tokens 免费

## 5. 效果与总结

### 提效数据

- 整个应用从零到可用，大约 **2-3 小时**的对话时间
- 涉及前后端共 **19 个功能点**，平均每个功能点约 10 分钟
- 其中约 80% 的功能一次通过，20% 有返工

### SOLO 在流程中做了什么

1. **全栈开发**：数据库 schema 设计、后端 API（tRPC）、前端组件（React + Tailwind）
2. **AI 对接**：阿里云通义千问 API 集成，system prompt 设计
3. **加密安全**：API Key 的 AES-256-GCM 加密存储方案
4. **智能匹配**：用 AI 判断主题与词库的相关性

### 可复用的经验

1. **逐步迭代比一次性提需求更高效**：先做核心功能跑通，再逐步加智能过滤、词库管理等高级功能
2. **对 AI 返回值要做防御性解析**：AI 可能返回多种格式，代码要兼容
3. **不同任务用独立的 AI 调用方法**：生成单词和匹配词库是两个不同的任务，不应该共用同一个 system prompt
4. **涉及多步骤交互的需求，先确认流程再写代码**：这是本次返工最多的地方
