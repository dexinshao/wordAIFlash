# WordFlash 开发对话总结与反思

> 生成时间：2026-04-24

---

## 一、需求清单与解决路径

| # | 需求 | 解决方案 | 返工次数 | 备注 |
|---|------|---------|---------|------|
| 1 | AI 生成的单词打上【四级】【六级】等标签 | 后端查 libraryWords 表，返回 tags | 0 | ✅ 一次通过 |
| 2 | AI 用阿里的哪个模型 | 推荐 qwen-turbo（每月 100 万 Tokens 免费） | 0 | ✅ 一次通过 |
| 3 | .env 里不该放明文 API Key | 清理 .env，只用加密文件 | 1 | ⚠️ 犯了错，用户指出才改 |
| 4 | 引导完成密钥配置 | 交互式收集 Key + 加密脚本 | 0 | ✅ 一次通过 |
| 5 | setup-secrets.ts 在 Mac 上报错 | 解释 node_modules 平台不匹配 | 0 | ✅ 无需操作 |
| 6 | 预览页支持删除单词 | 悬停显示 ✕ 删除按钮 | 0 | ✅ 一次通过 |
| 7 | 删除的单词标记为已掌握 | markWordsAsMastered API | 0 | ✅ 一次通过 |
| 8 | 不在词库中的单词添加到"已掌握"词库 | 自动创建 mastered 词库 | 0 | ✅ 一次通过 |
| 9 | AI 生成时判断相关词库，用户选择目标 | findRelatedLibraries + 5 步流程 | 0 | ✅ 一次通过 |
| 10 | 首页支持删除词库（可选合并） | library.delete API + 确认对话框 | 0 | ✅ 一次通过 |
| 11 | AI 生成时排除指定词库的单词 | excludeLibraryId 参数 | 0 | ✅ 一次通过 |
| 12 | 过滤后不足 10 个则重试 | 最多重试 3 次 | 0 | ✅ 一次通过 |
| 13 | 用阿里百炼记忆体存储已掌握单词 | 调研后发现不适合（需智能体应用、200 字符限制） | - | ❌ 放弃，用现有方案 |
| 14 | 把排除词库的单词告诉 AI（折中方案） | callAI 传 excludeWords（≤100） | 0 | ✅ 一次通过 |
| 15 | 选择目标词库默认选中第一个相关词库 | 自动选中逻辑 | 0 | ✅ 一次通过 |
| 16 | 排除词库下拉框默认选中相关词库 | 实时搜索 + useEffect | 1 | ⚠️ 方案被否决 |
| 17 | 用 AI 判断词库匹配（替代硬编码映射） | 先加了中文映射表，用户要求用 AI | 1 | ⚠️ 理解错意图 |
| 18 | 先选排除词库再生成（而非生成后匹配） | 先做成了生成后匹配，用户否决 | 1 | ⚠️ 流程理解反了 |
| 19 | 排除词库匹配不到 weather | callAI 的 system prompt 冲突 + ID 类型不匹配 | 2 | 🔧 调试后修复 |

---

## 二、被否决的方案

1. **硬编码中英文映射表**（天气→weather、旅行→travel...）→ 用户要求用 AI 判断
2. **生成单词后再匹配相关词库** → 用户要求先选排除词库再生成
3. **实时搜索（useQuery）** → 改为点击展开时才调用 AI（mutation）

---

## 三、返工根因分析

| 返工原因 | 次数 | 占比 |
|---------|------|------|
| 理解错了用户意图（流程顺序、方案方向） | 3 | 50% |
| 技术细节疏忽（明文 Key、ID 类型、prompt 冲突） | 3 | 50% |

---

## 四、反思与改进建议

### 用户可以改进的地方

1. **提前描述完整流程**：对于涉及多步骤交互的需求，一次性给出完整的用户操作流程，例如：
   > "用户流程是：输入主题 → 展开排除词库（AI 匹配）→ 选好后点生成 → 预览 → 选目标词库 → 创建"

2. **一次性给出约束条件**：如果对技术方案有偏好，在提需求时直接说明，例如：
   > "不要硬编码映射，要用 AI 判断匹配"
   > "不要用 useEffect 实时搜索，用按钮触发"

3. **区分"需求"和"实现方式"**：需求描述"要什么"，实现方式可以留给 AI 判断；但如果对实现方式有明确偏好，直接说

### AI 可以改进的地方

1. **动手前先确认理解**：对于流程类需求，先用文字复述完整流程让用户确认，再开始写代码
2. **记住已有上下文**：已经做了加密存储，就不该在 .env 里写明文 Key
3. **新功能用独立方法**：findRelatedLibraries 不应该复用 callAI（词汇生成专用），应该一开始就用独立的 AI 调用方法
4. **考虑 AI 返回值的不确定性**：AI 可能返回 `[9]`、`["9"]`、`[{"id":9}]` 等多种格式，解析时应兼容

---

## 五、一句话总结

> 大部分返工是因为"没理解用户的完整意图就开始写了"。以后对于涉及多步骤流程的需求，先列出完整流程让用户确认，再动手实现。
