# Ngrok 内网穿透部署问题记录

## 问题 1：Vite allowedHosts 拦截

**错误信息：**
```
Blocked request. This host ("heatful-pearlie-unmonotonously.ngrok-free.dev") is not allowed.
To allow this host, add "heatful-pearlie-unmonotonously.ngrok-free.dev" to `server.allowedHosts` in vite.config.js.
```

**原因：** Vite 开发服务器默认只允许 `localhost` 域名访问，ngrok 生成的公网域名被拦截。

**解决方案：** 在 `vite.config.ts` 中添加 `allowedHosts: true`：

```typescript
server: {
  host: "0.0.0.0",
  port: 3000,
  allowedHosts: true,  // 允许所有域名访问
},
```

---

## 问题 2：esbuild 平台不匹配

**错误信息：**
```
Error [TransformError]:
You installed esbuild for another platform than the one you're currently using.
Specifically the "@esbuild/linux-x64" package is present but this platform
needs the "@esbuild/darwin-x64" package instead.
```

**原因：** `node_modules` 是在 Linux VM 上安装的，拷贝到 Mac 上后二进制文件不兼容。

**解决方案：** 删除 `node_modules` 重新安装：

```bash
rm -rf node_modules
npm install
```

---

## 问题 3：Railway CLI 安装超时

**错误信息：**
```
npm error FetchError: request to https://github.com/railwayapp/cli/releases/download/v4.42.0/railway-v4.42.0-x86_64-apple-darwin.tar.gz failed, reason: connect ETIMEDOUT 20.205.243.166:443
```

**原因：** GitHub 被墙，无法下载 Railway CLI 的二进制文件。

**解决方案：** 开代理后安装：

```bash
export https_proxy=http://127.0.0.1:7890
npm install -g @railway/cli
```

---

## 问题 4：Railway `railway init` 报 "Connected branch does not exist"

**错误信息：**
```
Connected branch does not exist
```

**原因：** 项目目录下没有初始化 Git 仓库。

**解决方案：** 先初始化 Git：

```bash
git init
git add .
git commit -m "init"
railway init
```

---

## 问题 5：Railway `railway variables set` 报 "No service linked"

**错误信息：**
```
No service linked
Run `railway service` to link a service
```

**原因：** 项目中还没有创建 service，需要先在 Railway 网页上创建。

**解决方案：**
1. 打开 Railway 项目页面
2. 点击 **Add Service** → **Dockerfile**
3. 回到终端执行 `railway service` 关联

---

## 问题 6：Railway `git push` 报 "没有配置推送目标"

**错误信息：**
```
致命错误：没有配置推送目标。
```

**原因：** Git 仓库没有添加远程仓库地址。

**解决方案：** 先添加远程仓库：

```bash
# GitHub（需要代理）
git remote add origin https://github.com/用户名/wordflash.git
git push -u origin main

# 或 Gitee（国内可直接访问）
git remote add origin https://gitee.com/用户名/wordflash.git
git push -u origin main
```

---

## 问题 7：Railway 部署后 API 返回 500

**错误信息：**
```
/api/trpc/library.list → httpStatus 500
```

**原因：** Docker 使用 Alpine Linux，缺少 `sqlite-dev`，导致 `better-sqlite3` 编译失败。

**解决方案：** 在 Dockerfile 中添加 `sqlite-dev`：

```dockerfile
FROM node:20-alpine AS base
RUN apk add --no-cache python3 make g++ sqlite-dev
```

---

## 问题 8：Railway 部署后生成词库报 JSON 解析错误

**错误信息：**
```
Failed to execute 'json' on 'Response': Unexpected end of JSON input
```

**原因：** Railway 上的环境变量没有配置完整，AI API 调用失败，后端返回空响应，前端解析 JSON 报错。

**解决方案：** 在 Railway 网页 → service → Variables 中确认以下变量都已设置：

| 变量名 | 值 |
|--------|-----|
| `APP_ID` | `wordflash` |
| `APP_SECRET` | 随便一个随机字符串 |
| `DATABASE_URL` | `sqlite:./data/wordflash.db` |
| `AI_BASE_URL` | `https://dashscope.aliyuncs.com/compatible-mode/v1` |
| `AI_API_KEY` | 你的阿里云 API Key（`sk-` 开头） |
| `AI_MODEL` | `qwen-turbo` |

---

## 问题 9：ngrok 报 ERR_NGROK_8012 连接被拒绝

**错误信息：**
```
ERR_NGROK_8012
Traffic successfully made it to the ngrok agent, but the agent failed to establish
a connection to the upstream web service at http://localhost:3000.
The error encountered was: dial tcp [::1]:3000: connect: connection refused
```

**原因：** 项目没有启动，或者 ngrok 默认尝试连接 IPv6 地址 `[::1]:3000`，而项目监听的是 IPv4 `0.0.0.0`。

**解决方案：**

1. 先确保项目已启动：`npm run dev`
2. 用 IPv4 地址启动 ngrok：

```bash
ngrok http 127.0.0.1:3000
```

---

---

## 问题 10：Railway 部署后 API 返回 500（SQLite 文件系统限制）

**错误信息：**
```
/api/trpc/library.list → httpStatus 500
```

**原因：** Railway 使用临时文件系统，每次部署后 SQLite 数据库文件丢失，且 Alpine Linux 容器中 `data/` 目录不存在导致数据库无法创建。

**解决方案：** 从 SQLite 迁移到 MySQL/PostgreSQL 等网络数据库。

---

## 问题 11：SQLite 迁移到 MySQL 后 Drizzle 配置不兼容

**错误信息：**
```
dialect "sqlite" 不支持 MySQL 连接
```

**原因：** `drizzle.config.ts` 中 `dialect: "sqlite"` 和 `dbCredentials` 配置不匹配 MySQL。

**解决方案：** 修改 `drizzle.config.ts`：

```typescript
import { defineConfig } from "drizzle-kit";

export default defineConfig({
  schema: "./db/schema.ts",
  out: "./db/migrations",
  dialect: "mysql",  // 从 sqlite 改为 mysql
  dbCredentials: {
    url: process.env.DATABASE_URL,  // mysql://user:pass@host:3306/db
  },
});
```

---

## 问题 12：SQLite 迁移到 MySQL 后 schema 语法不兼容

**错误信息：**
```
sqliteTable is not defined
integer is not defined
```

**原因：** `db/schema.ts` 中使用的是 SQLite 特有的 `sqliteTable`、`integer`、`text` 等，MySQL 需要 `mysqlTable`、`int`、`varchar` 等。

**解决方案：** 修改 `db/schema.ts`：

```typescript
// 修改前（SQLite）
import { sqliteTable, integer, text, real } from "drizzle-orm/sqlite-core";

export const words = sqliteTable("words", {
  id: integer("id").primaryKey({ autoIncrement: true }),
  word: text("word").notNull(),
  frequencyRank: integer("frequency_rank"),
  createdAt: integer("created_at", { mode: "timestamp" }).notNull(),
});

// 修改后（MySQL）
import { mysqlTable, int, text, varchar, timestamp } from "drizzle-orm/mysql-core";

export const words = mysqlTable("words", {
  id: int("id").primaryKey().autoincrement(),
  word: varchar("word", { length: 100 }).notNull(),
  frequencyRank: int("frequency_rank"),
  createdAt: timestamp("created_at").notNull().defaultNow(),
});
```

---

## 问题 13：SQLite 迁移到 MySQL 后 `lastInsertRowid` 报错

**错误信息：**
```
Cannot read property 'lastInsertRowid' of undefined
```

**原因：** SQLite 的 `better-sqlite3` 返回 `lastInsertRowid`，而 MySQL 的 `mysql2` 返回 `[ResultSetHeader]`，其中 `insertId` 才是自增 ID。

**解决方案：** 修改所有获取自增 ID 的代码：

```typescript
// 修改前（SQLite）
const wordId = Number((inserted as any).lastInsertRowid);

// 修改后（MySQL）
const wordId = Number((inserted as any)[0].insertId);
```

---

## 问题 14：SQLite 迁移到 MySQL 后布尔值不兼容

**错误信息：**
```
value.toISOString is not a function
```
（或类似的类型转换错误）

**原因：** SQLite 用 `1/0` 表示布尔值，MySQL 用 `true/false`。代码中 `isBuiltin: 1` 需要改为 `isBuiltin: true`。

**解决方案：**

```typescript
// 修改前（SQLite）
.where(eq(wordLibraries.isBuiltin, 1))

// 修改后（MySQL）
.where(eq(wordLibraries.isBuiltin, true))
```

---

## 问题 15：SQLite 迁移到 MySQL 后日期格式不兼容

**错误信息：**
```
value.toISOString is not a function
```

**原因：** SQLite 用文本存储 ISO 日期字符串（如 `"2025-01-15T10:30:00.000Z"`），代码中传 `new Date().toISOString()`。MySQL 的 `TIMESTAMP` 类型需要 `Date` 对象。

**解决方案：** 将所有 `new Date().toISOString()` 改为 `new Date()`：

```typescript
// 修改前（SQLite）
.set({
  lastReviewedAt: new Date().toISOString(),
  updatedAt: new Date().toISOString(),
})

// 修改后（MySQL）
.set({
  lastReviewedAt: new Date(),
  updatedAt: new Date(),
})
```

---

## 问题 16：MySQL 连接使用单连接导致并发问题

**问题现象：** 后台任务 `enrichWordInBackground` 执行时，其他数据库查询卡住。

**原因：** `getDb()` 使用 `mysql.createConnection()` 创建单连接，多个并发查询会互相阻塞。

**解决方案：** 使用连接池替代单连接：

```typescript
// 修改前（单连接）
import mysql from "mysql2/promise";
const connection = await mysql.createConnection(url);

// 修改后（连接池）
import mysql from "mysql2/promise";
const pool = mysql.createPool({
  uri: url,
  waitForConnections: true,
  connectionLimit: 10,
  queueLimit: 0,
});
```

---

## 完整部署流程（Mac 本地 → Railway）

```bash
# 1. 安装依赖
cd /Users/zhaoyaxin/Documents/trae_projects/wordFlash/app
rm -rf node_modules
npm install

# 2. 本地运行测试（连接本地 MySQL Docker）
npm run dev

# 3. 另开终端，启动 ngrok（注意用 127.0.0.1 而非 localhost）
ngrok http 127.0.0.1:3000

# 4. Railway 部署（使用 Railway MySQL）
git init && git add . && git commit -m "init"
railway init
# 网页上创建 Dockerfile service
# 网页上添加 MySQL 插件
railway service
railway variables set APP_ID=wordflash
railway variables set APP_SECRET=随机字符串
railway variables set DATABASE_URL=mysql://root:password@mysql.railway.internal:3306/railway
railway variables set AI_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
railway variables set AI_API_KEY=你的API Key
railway variables set AI_MODEL=qwen-turbo
railway up
```
