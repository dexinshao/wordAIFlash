# WordFlash 项目问题记录与解决方案

## 目录

- [一、代码相关问题](#section-1-code)
  - [1.1 词库导入 - 重复单词问题](#section-1-1)
  - [1.2 词库导入 - 派生词词频继承](#section-1-2)
  - [1.3 词库导入 - 无词频单词排序](#section-1-3)
  - [1.4 学习算法 - 首次学习顺序](#section-1-4)
  - [1.5 学习算法 - 预取导致重复单词](#section-1-5)
  - [1.6 API/后端 - MySQL 语法兼容性](#section-1-6)
  - [1.7 API/后端 - 已存在单词不更新词频](#section-1-7)
  - [1.8 数据库 - 重复数据清理](#section-1-8)
- [二、UI/动画相关问题](#section-2-ui)
  - [2.1 卡片切换闪烁（反面闪现）](#section-2-1)
  - [2.2 卡片切换卡顿](#section-2-2)
  - [2.3 反馈按钮点击后闪烁](#section-2-3)
  - [2.4 已掌握数字对齐问题](#section-2-4)
- [三、本地开发环境问题](#section-3-local)
  - [3.1 Vite allowedHosts 拦截（ngrok）](#section-3-1)
  - [3.2 esbuild 平台不匹配](#section-3-2)
  - [3.3 ngrok 连接被拒绝 ERR_NGROK_8012](#section-3-3)
  - [3.4 Git 未配置 user.name 和 user.email](#section-3-4)
  - [3.5 Git push 报 HTTP 400 / 远端意外挂断](#section-3-5)
  - [3.6 Git detached HEAD（分离头指针）](#section-3-6)
  - [3.7 工作目录混淆](#section-3-7)
- [四、Railway 部署问题](#section-4-railway)
  - [4.1 Railway CLI 安装问题](#section-4-1)
  - [4.2 Git 与推送问题](#section-4-2)
  - [4.3 构建与部署问题](#section-4-3)
  - [4.4 数据库问题](#section-4-4)
  - [4.5 环境变量问题](#section-4-5)
  - [4.6 网络与监听问题](#section-4-6)
- [五、经验总结](#section-5-summary)
- [六、部署流程清单](#section-6-checklist)

---

<a name="section-1-code"></a>
## 一、代码相关问题

### 1.1 词库导入 - 重复单词问题
**问题描述**：COCA 高频10000词文件中，同一单词因不同词性出现多次（如 stall 出现两次，排名分别为 #6769 和 #6897）。

**解决方案**：
- 修改数据库唯一索引：从 `(word)` 单字段改为 `(word, frequency_rank)` 联合唯一索引
- 导入逻辑区分处理：
  - 有词频的单词：按 `(word, frequencyRank)` 查重，允许同一单词不同排名共存
  - 无词频的单词：按 `(word, 99999)` 查重，已存在则跳过

### 1.2 词库导入 - 派生词词频继承
**问题描述**：vitally 等派生词没有词频排名，但含义与词根 vital 高度相关。

**解决方案**：
- 实现 `deriveStem()` 函数，支持 24 种常见后缀（-ly, -ness, -tion, -ment, -able, -ful, -less, -ive, -ity, -ism 等）
- 导入时，无词频单词自动尝试从词根继承 `frequencyRank`
- 数据库批量处理：对已有无词频单词跑了一次继承，成功更新 47 个

### 1.3 词库导入 - 无词频单词排序
**问题描述**：无词频的单词默认排名为 0，导致这些生僻词被优先学习。

**解决方案**：
- 将无词频单词的默认排名从 0 改为 99999
- 前端展示条件：`frequencyRank > 0 && frequencyRank < 99999` 才显示排名标签
- 学习顺序：按 `frequencyRank` 升序，99999 排在最后

### 1.4 学习算法 - 首次学习顺序
**问题描述**：原算法使用加权随机，导致高频词不一定先学。

**解决方案**：
- 修改 `getNextFlashcard` 逻辑：
  - 分离未学过和已学过的单词
  - 未学过的按 `frequencyRank` 升序取第一个
  - 已学过的保持加权随机算法

### 1.5 学习算法 - 预取导致重复单词
**问题描述**：预取下一张卡片时，可能取到当前正在学习的单词。

**解决方案**：
- 后端 `getNextFlashcard` 新增 `excludeWordId` 参数
- 前端预取时传入当前单词 ID，后端查询时排除该单词

### 1.6 API/后端 - MySQL 语法兼容性
**问题描述**：`getMasteredWords` 端点使用 `NULLS LAST`，但 MySQL 不支持。

**错误信息**：SQL 语法错误

**解决方案**：将 `ORDER BY frequency_rank ASC NULLS LAST` 改为 MySQL 兼容的 `ORDER BY frequency_rank IS NULL, frequency_rank ASC`

### 1.7 API/后端 - 已存在单词不更新词频
**问题描述**：重新导入带词频的词库时，已存在的单词没有更新 `frequencyRank`。

**解决方案**：修改导入逻辑，已存在的单词如果 `frequencyRank` 为空，则自动更新。

### 1.8 数据库 - 重复数据清理
**问题描述**：数据库中存在重复单词（如 abundant 有两条记录，一条有 frequency_rank，一条没有）。

**解决方案**：
- 清理重复数据：将 `libraryWords` 关联迁移到有排名的记录
- 添加唯一索引：`(word, frequency_rank)` 防止再次产生重复

---

<a name="section-2-ui"></a>
## 二、UI/动画相关问题

### 2.1 卡片切换闪烁（反面闪现）
**问题描述**：新卡片滑入时，先显示反面再快速切换到正面。

**根本原因**：
- `setIsFlipped(false)` 和 `setIsAnimating(false)` 在同一 React 批量更新中
- 浏览器渲染时 `isFlipped` 仍为旧值 `true`，导致 `rotate-y-180` 生效

**解决方案**：
- 修改 CSS：动画期间禁用 transition，只在 `!isAnimating` 时启用
- 修改 `rotate-y-180` 条件：只在 `isFlipped && !isAnimating` 时添加

### 2.2 卡片切换卡顿
**问题描述**：点击反馈后，新卡片滑入有延迟感。

**解决方案**：
- 实现预取机制：学习当前卡片时后台预取下一张
- 并行加载：`setIsAnimating(false)` 和 `fetchNextWord()` 同时执行
- 不等 API 返回，立即开始滑入动画

### 2.3 反馈按钮点击后闪烁
**问题描述**：点击已掌握等反馈按钮时，卡片会再闪一次才切换。

**根本原因**：`fetchNextWord` 是异步的，但 `setIsAnimating(false)` 同步执行，导致卡片在数据加载前恢复可见。

**解决方案**：
- 预取机制确保数据已在内存中
- `fetchNextWord` 同步返回预取结果

### 2.4 已掌握数字对齐问题
**问题描述**：首页统计卡片中，"已掌握"的数字和文字没有居中对齐。

**根本原因**：使用 `flex` 包裹数字和下载图标，影响居中对齐。

**解决方案**：将下载图标改为绝对定位，不影响数字的居中对齐。

---

<a name="section-3-local"></a>
## 三、本地开发环境问题

### 3.1 Vite allowedHosts 拦截（ngrok）
**错误信息**：
```
Blocked request. This host ("xxx.ngrok-free.dev") is not allowed.
To allow this host, add "xxx.ngrok-free.dev" to `server.allowedHosts` in vite.config.js.
```

**原因：** Vite 开发服务器默认只允许 `localhost` 域名访问，ngrok 生成的公网域名被拦截。

**解决方案：** 在 `vite.config.ts` 中添加 `allowedHosts: true`：
```typescript
server: {
  host: "0.0.0.0",
  port: 3000,
  allowedHosts: true,  // 允许所有域名访问
},
```

### 3.2 esbuild 平台不匹配
**错误信息**：
```
Error [TransformError]: You installed esbuild for another platform than the one you're currently using.
Specifically the "@esbuild/linux-x64" package is present but this platform needs the "@esbuild/darwin-x64" package instead.
```

**原因：** `node_modules` 是在 Linux VM 上安装的，拷贝到 Mac 上后二进制文件不兼容。

**解决方案：** 删除 `node_modules` 重新安装：
```bash
rm -rf node_modules
npm install
```

### 3.3 ngrok 连接被拒绝 ERR_NGROK_8012
**错误信息**：
```
ERR_NGROK_8012
Traffic successfully made it to the ngrok agent, but the agent failed to establish a connection to the upstream web service at http://localhost:3000.
```

**原因：** 项目没有启动，或者 ngrok 默认尝试连接 IPv6 地址 `[::1]:3000`，而项目监听的是 IPv4 `0.0.0.0`。

**解决方案：**
```bash
# 1. 先确保项目已启动
npm run dev

# 2. 用 IPv4 地址启动 ngrok
ngrok http 127.0.0.1:3000
```

### 3.4 Git 未配置 user.name 和 user.email
**解决方案：**
```bash
git config --global user.name "你的用户名"
git config --global user.email "你的邮箱"
```

### 3.5 Git push 报 HTTP 400 / 远端意外挂断
**原因：** 推送数据量超过 Git HTTP 缓冲区大小。

**解决方案：**
```bash
# 增大 HTTP 缓冲区到 500MB
git config --global http.postBuffer 524288000

# 重新推送
git push -u origin main
```

### 3.6 Git detached HEAD（分离头指针）
**错误信息**：`致命错误：您当前不在一个分支上。`

**解决方案：**
```bash
# 强制将 main 分支指向当前 HEAD
git checkout -f main
git reset --hard <最新提交的SHA>
git push -u origin main --force
```

### 3.7 工作目录混淆
**问题描述**：修改的文件没有生效，因为实际运行的是另一个目录的代码。

**解决方案**：确认路径关系后，统一在正确路径下修改。

---

<a name="section-4-railway"></a>
## 四、Railway 部署问题

### 4.1 Railway CLI 安装问题

#### 4.1.1 CLI 安装超时
**错误信息**：`connect ETIMEDOUT 20.205.243.166:443`

**原因：** GitHub 被墙，无法下载 Railway CLI 的二进制文件。

**解决方案：**
```bash
export https_proxy=http://127.0.0.1:7890
npm install -g @railway/cli
```

#### 4.1.2 CLI 安装后 permission denied
**错误信息**：`zsh: permission denied: railway`

**解决方案：**
```bash
chmod +x $(npm prefix -g)/bin/railway
```

#### 4.1.3 `railway init` 报 "Connected branch does not exist"
**原因：** 项目目录下没有初始化 Git 仓库。

**解决方案：**
```bash
git init && git add . && git commit -m "init"
railway init
```

#### 4.1.4 `railway variables set` 报 "No service linked"
**原因：** 项目中还没有创建 service。

**解决方案：**
1. 打开 Railway 项目页面
2. 点击 **Add Service** → **Dockerfile**
3. 回到终端执行 `railway service` 关联

### 4.2 Git 与推送问题

#### 4.2.1 HTTPS 方式推送 GitHub 失败
**解决方案：** 改用 SSH 密钥认证：
```bash
# 1. 生成密钥
ssh-keygen -t ed25519 -C "SOLO Push Key"

# 2. 将公钥添加到 GitHub → Settings → SSH Keys

# 3. 修改远程地址
git remote set-url origin git@github.com:dexinshao/wordAIFlash.git
```

#### 4.2.2 Docker 构建失败 - package.json not found
**错误信息**：`failed to calculate checksum: "/package.json": not found`

**原因：** Railway 默认从仓库根目录构建，但 `package.json` 在 `app/` 子目录下。

**解决方案：** 在 Railway 控制台设置 **Root Directory** 为 `app`

### 4.3 构建与部署问题

#### 4.3.1 使用了错误的构建器（NIXPACKS 而非 Dockerfile）
**解决方案：** 修改 `railway.json`：
```json
{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE"
  }
}
```

#### 4.3.2 SQLite 缺少编译依赖
**错误信息：** Docker 构建时 `better-sqlite3` 编译失败

**解决方案：** 在 Dockerfile 中添加：
```dockerfile
FROM node:20-alpine AS base
RUN apk add --no-cache python3 make g++ sqlite-dev
```

### 4.4 数据库问题

#### 4.4.1 数据库类型不匹配（代码用 MySQL，Railway 用 Postgres）
**现象：** 页面报错，API 请求失败

**原因：** Railway 上初始创建的是 Postgres 服务，但代码使用 mysql2 驱动

**解决方案：** 在 Railway 页面上手动添加 MySQL 服务，删除或忽略原有 Postgres

#### 4.4.2 数据库表未创建，应用启动即崩溃
**现象：** MySQL 服务已就绪，但应用反复重启

**原因：** MySQL 数据库为空，没有任何表

**最终解决方案：** 应用启动时自动初始化
- 新建 `db/auto-init.ts`：使用原生 SQL 创建表和 seed 数据
- 修改 `api/boot.ts`：启动 HTTP 服务前调用 `autoInitDatabase()`
- 简化 Dockerfile，移除不必要的依赖

#### 4.4.3 DATABASE_URL 环境变量缺失
**错误信息：** `Error: Missing required environment variable: DATABASE_URL`

**原因：** Railway MySQL 服务提供的默认变量名是 `MYSQL_URL`，不是 `DATABASE_URL`

**解决方案：** 在 wordAIFlash 服务的 Variables 页面手动添加：
- **Name**: `DATABASE_URL`
- **Value**: `${{MySQL.MYSQL_URL}}`

#### 4.4.4 SQLite 迁移到 MySQL 的兼容性问题

| 问题 | 解决方案 |
|------|---------|
| Drizzle 配置不兼容 | `dialect: "mysql"`，使用 `dbCredentials: { url: process.env.DATABASE_URL }` |
| Schema 语法不兼容 | `sqliteTable` → `mysqlTable`，`integer` → `int`，`text` → `varchar` |
| `lastInsertRowid` 报错 | SQLite 返回 `.lastInsertRowid`，MySQL 返回 `[0].insertId` |
| 布尔值不兼容 | SQLite 用 `1/0`，MySQL 用 `true/false` |
| 日期格式不兼容 | SQLite 用 `toISOString()`，MySQL 直接用 `new Date()` |

#### 4.4.5 MySQL 连接使用单连接导致并发问题
**问题现象：** 后台任务执行时，其他数据库查询卡住

**解决方案：** 使用连接池替代单连接：
```typescript
const pool = mysql.createPool({
  uri: url,
  waitForConnections: true,
  connectionLimit: 10,
  queueLimit: 0,
});
```

### 4.5 环境变量问题

#### 4.5.1 应用启动失败 - Missing required environment variable
**错误信息：** `Error: Missing required environment variable: APP_ID`

**解决方案：** 在 Railway 控制台 → Variables 中添加：

| 变量名 | 值 |
|--------|-----|
| `APP_ID` | `wordflash` |
| `APP_SECRET` | 随机字符串（`openssl rand -hex 32`） |
| `DATABASE_URL` | `${{MySQL.MYSQL_URL}}` |
| `AI_BASE_URL` | `https://dashscope.aliyuncs.com/compatible-mode/v1` |
| `AI_API_KEY` | 你的阿里云 API Key |
| `AI_MODEL` | `qwen-turbo` |

#### 4.5.2 生成词库报 JSON 解析错误
**错误信息：** `Failed to execute 'json' on 'Response': Unexpected end of JSON input`

**原因：** Railway 上的环境变量没有配置完整，AI API 调用失败

**解决方案：** 确认所有必需的环境变量都已设置

### 4.6 网络与监听问题

#### 4.6.1 localhost:8080 是否有问题？
**现象：** 日志显示 `Server running on http://localhost:8080/`

**分析：** `@hono/node-server` 当 `hostname` 为 `undefined` 时，底层绑定 `0.0.0.0`。日志中的 `localhost` 只是显示文本，不影响实际监听地址。

**结论：** 无需修改，这是正常行为。

---

<a name="section-5-summary"></a>
## 五、经验总结

### 5.1 代码开发经验
- **联合唯一索引**：使用 `(word, frequency_rank)` 防止重复数据
- **预取机制**：减少卡片切换等待时间，提升用户体验
- **派生词处理**：自动继承词频，减少数据缺失

### 5.2 本地开发经验
- **跨平台依赖**：`node_modules` 在不同平台间需要重新安装
- **ngrok 使用**：使用 `127.0.0.1` 而非 `localhost` 避免 IPv6 问题
- **Vite 配置**：`allowedHosts: true` 允许外部域名访问

### 5.3 Railway 部署经验
- **数据库类型要一致**：代码用什么数据库，Railway 就要创建对应类型的服务
- **preDeployCommand 不可靠**：在旧容器中执行，不适合需要运行时工具的场景
- **esbuild 不能打包原生模块**：mysql2、better-sqlite3 等含 C++ 代码的模块无法 bundle
- **启动时自初始化最可靠**：将建表和 seed 逻辑写入应用启动流程
- **MySQL 变量引用格式**：Railway MySQL 的变量名是 `MYSQL_URL`，需要手动映射到 `DATABASE_URL`
- **SSH 密钥优于 HTTPS**：自动化环境中 SSH 认证更方便

---

<a name="section-6-checklist"></a>
## 六、部署流程清单

### 本地开发
```bash
# 1. 安装依赖
npm install

# 2. 本地运行测试
npm run dev

# 3. 启动 ngrok（使用 127.0.0.1）
ngrok http 127.0.0.1:3000
```

### Railway 部署
```bash
# 1. 初始化 Git
git init && git add . && git commit -m "init"

# 2. Railway 初始化
railway init

# 3. 网页操作
# - 创建 Dockerfile service
# - 添加 MySQL 插件
# - 设置 Root Directory 为 app
# - 配置环境变量 DATABASE_URL=${{MySQL.MYSQL_URL}}

# 4. 关联服务并部署
railway service
railway up
```
