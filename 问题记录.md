# WordFlash 项目问题记录与解决方案

---

## 一、词库导入相关问题

### 1.1 重复单词问题
**问题描述**：COCA 高频10000词文件中，同一单词因不同词性出现多次（如 stall 出现两次，排名分别为 #6769 和 #6897）。

**解决方案**：
- 修改数据库唯一索引：从 `(word)` 单字段改为 `(word, frequency_rank)` 联合唯一索引
- 导入逻辑区分处理：
  - 有词频的单词：按 `(word, frequencyRank)` 查重，允许同一单词不同排名共存
  - 无词频的单词：按 `(word, 99999)` 查重，已存在则跳过

### 1.2 派生词词频继承
**问题描述**：vitally 等派生词没有词频排名，但含义与词根 vital 高度相关。

**解决方案**：
- 实现 `deriveStem()` 函数，支持 24 种常见后缀（-ly, -ness, -tion, -ment, -able, -ful, -less, -ive, -ity, -ism 等）
- 导入时，无词频单词自动尝试从词根继承 `frequencyRank`
- 数据库批量处理：对已有无词频单词跑了一次继承，成功更新 47 个

### 1.3 无词频单词排序
**问题描述**：无词频的单词默认排名为 0，导致这些生僻词被优先学习。

**解决方案**：
- 将无词频单词的默认排名从 0 改为 99999
- 前端展示条件：`frequencyRank > 0 && frequencyRank < 99999` 才显示排名标签
- 学习顺序：按 `frequencyRank` 升序，99999 排在最后

---

## 二、学习算法相关问题

### 2.1 首次学习顺序
**问题描述**：原算法使用加权随机，导致高频词不一定先学。

**解决方案**：
- 修改 `getNextFlashcard` 逻辑：
  - 分离未学过和已学过的单词
  - 未学过的按 `frequencyRank` 升序取第一个
  - 已学过的保持加权随机算法

### 2.2 预取导致重复单词
**问题描述**：预取下一张卡片时，可能取到当前正在学习的单词。

**解决方案**：
- 后端 `getNextFlashcard` 新增 `excludeWordId` 参数
- 前端预取时传入当前单词 ID，后端查询时排除该单词

---

## 三、UI/动画相关问题

### 3.1 卡片切换闪烁（反面闪现）
**问题描述**：新卡片滑入时，先显示反面再快速切换到正面。

**根本原因**：
- `setIsFlipped(false)` 和 `setIsAnimating(false)` 在同一 React 批量更新中
- 浏览器渲染时 `isFlipped` 仍为旧值 `true`，导致 `rotate-y-180` 生效

**解决方案**：
- 修改 CSS：动画期间禁用 transition，只在 `!isAnimating` 时启用
- 修改 `rotate-y-180` 条件：只在 `isFlipped && !isAnimating` 时添加

### 3.2 卡片切换卡顿
**问题描述**：点击反馈后，新卡片滑入有延迟感。

**解决方案**：
- 实现预取机制：学习当前卡片时后台预取下一张
- 并行加载：`setIsAnimating(false)` 和 `fetchNextWord()` 同时执行
- 不等 API 返回，立即开始滑入动画

### 3.3 反馈按钮点击后闪烁
**问题描述**：点击已掌握等反馈按钮时，卡片会再闪一次才切换。

**根本原因**：`fetchNextWord` 是异步的，但 `setIsAnimating(false)` 同步执行，导致卡片在数据加载前恢复可见。

**解决方案**：
- 预取机制确保数据已在内存中
- `fetchNextWord` 同步返回预取结果

### 3.4 已掌握数字对齐问题
**问题描述**：首页统计卡片中，"已掌握"的数字和文字没有居中对齐。

**根本原因**：使用 `flex` 包裹数字和下载图标，影响居中对齐。

**解决方案**：将下载图标改为绝对定位，不影响数字的居中对齐。

---

## 四、数据相关问题

### 4.1 数据库重复数据
**问题描述**：数据库中存在重复单词（如 abundant 有两条记录，一条有 frequency_rank，一条没有）。

**解决方案**：
- 清理重复数据：将 `libraryWords` 关联迁移到有排名的记录
- 添加唯一索引：`(word, frequency_rank)` 防止再次产生重复

### 4.2 词频排名不显示
**问题描述**：已导入 COCA 词库，但学习时卡片不显示词频排名。

**根本原因**：
1. 数据库中 `frequency_rank` 为 NULL 的单词占大多数
2. 工作目录混淆：修改的文件和实际运行的目录不一致

**解决方案**：
- 确认工作目录统一
- 批量更新：将已有单词的 `frequency_rank` 从 COCA 数据补充
- 派生词继承：为无词频单词补充排名

---

## 五、API/后端相关问题

### 5.1 MySQL 语法兼容性
**问题描述**：`getMasteredWords` 端点使用 `NULLS LAST`，但 MySQL 不支持。

**错误信息**：SQL 语法错误

**解决方案**：将 `ORDER BY frequency_rank ASC NULLS LAST` 改为 MySQL 兼容的 `ORDER BY frequency_rank IS NULL, frequency_rank ASC`

### 5.2 已存在单词不更新词频
**问题描述**：重新导入带词频的词库时，已存在的单词没有更新 `frequencyRank`。

**解决方案**：修改导入逻辑，已存在的单词如果 `frequencyRank` 为空，则自动更新。

---

## 六、文件路径问题

### 6.1 工作目录混淆
**问题描述**：修改的文件没有生效，因为实际运行的是另一个目录的代码。

**发现过程**：
- 发现两个路径：`/sessions/69ee0365df5e359506361089/` 和 `/sessions/69eb7be2a41f9b820632e0ad/`
- 经检查，两个路径实际指向同一文件（符号链接或挂载）

**解决方案**：确认路径关系后，统一在正确路径下修改。

---

## 七、优化总结

### 7.1 性能优化
- 预取机制减少卡片切换等待时间
- 批量数据库查询减少往返次数
- 索引优化加速词频排序查询

### 7.2 用户体验优化
- 卡片切换动画流畅，无闪烁
- 高频词优先学习，符合认知规律
- 派生词自动继承词频，减少数据缺失
- 已掌握单词可导出，便于复习

### 7.3 数据完整性优化
- 联合唯一索引防止重复数据
- 自动更新缺失的词频信息
- 全局统计去重计算
