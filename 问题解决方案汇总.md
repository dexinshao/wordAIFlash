# WordFlash 问题与解决方案汇总

记录截止目前（2026-05-20）开发过程中遇到的所有问题及解决方案。

---

## 一、环境与工具类

### 1.1 Cloud Studio 沙箱环境限制

**问题**：无法直接上传/下载文件，GitHub 授权流程无法在浏览器完成。

**解决方案**：
- 上传：将代码推送到 GitHub 公开仓库，在沙箱内 `git clone` 拉取
- 下载：将文件放入 `frontend/public/` 目录，通过预览 URL 访问下载
- GitHub 授权：使用 Classic PAT（勾选 `repo` 权限），通过 `git remote set-url` 嵌入 Token 推送

**教训**：Fine-grained Token 容易遗漏权限，Classic Token 勾选 `repo` 最省事。

---

## 二、小程序域名与网络类

### 2.1 微信小程序 request 合法域名报错

**报错**：`http://localhost:3000 不在以下 request 合法域名列表中`

**原因**：微信小程序不支持请求 localhost，且后端地址 `http://localhost:3000` 无法在小程序环境中访问。

**解决方案**：改为**前端直连**，不通过后端代理。
- AI 调用：前端直接用 `Taro.request` 调 CloudBase AI 接口
- 有道词典：前端直接用 `Taro.request` 调 `https://dict.youdao.com/suggest`
- 词库解析：前端直接读取 txt 文件内容并解析，不再上传到后端

**最终方案**：完全移除后端依赖，所有功能改为前端直连。

### 2.2 微信小程序需要配置的合法域名

如使用了网络请求，需在 [微信公众平台](https://mp.weixin.qq.com) → 开发管理 → 开发设置 → 服务器域名 中添加：

| 域名 | 用途 |
|------|------|
| `https://wordflash-d9gajqwvq157a1b06.api.tcloudbasegateway.com` | CloudBase AI 调用 |
| `https://dict.youdao.com` | 有道词典释义 |

---

## 三、后端移除相关

### 3.1 词库初始化超时（Error: timeout）

**报错**：`Error: timeout at Function.<anonymous> (WAServiceMainContext.js)`

**原因**：`initializeWordData()` 是异步的，但 `app.tsx` 的 `componentDidMount` 没有 `await`，导致页面在数据加载完成前就渲染，词库显示为空，点击学习直接显示"学习完成"。

**解决方案**：
- `componentDidMount` 改为 `async`，`await initializeWordData()`
- 添加全局 loading 页面，阻塞渲染直到数据加载完成
- 添加进度条（`_loadProgress` 变量，5% → 92% → 100%）

### 3.2 后端 API Key 安全问题

**问题**：用户问"小程序代码是否容易被反编译看到 API Key？"

**分析**：微信小程序打包为 `.wxapkg`，可以被反编译提取代码，API Key 有泄露风险。

**决策**：用户选择**不加密**（"不用了"），原因是 CloudBase API Key 仅能调用混元 AI，不涉及金融/用户敏感数据，风险可控。

**实现**：API Key 直接写在前端 `ai.ts` 中，通过 `Authorization: Bearer {API_KEY}` 调用。

---

## 四、CloudBase AI 类

### 4.1 CloudBase AI Token 额度耗尽（429）

**报错**：`429 额度已用尽`

**原因**：最初使用 `hunyuan-turbo`（hunyuan-exp 分组），体验额度 10,000 Token 很快耗尽。

**排查**：登录 CloudBase 控制台查看 AI 资源用量，发现：
- `hunyuan-exp` 分组：额度已用尽
- `hunyuan-v3` 分组：**100,100,000 Token 完全未使用**

**解决方案**：切换至 `hy3-preview` 模型（hunyuan-v3 分组）。

**教训**：小程序成长计划赠送的 Token 是按**模型分组**隔离的，用之前先确认哪个分组有额度。

### 4.2 CloudBase AI 认证方式

**问题**：最初尝试用 CloudBase Auth Proxy 代理认证，沙箱环境返回 401。

**原因**：沙箱环境未绑定腾讯云 OAuth 账号，auth-proxy 无法获取有效 Token。

**解决方案**：改为直接使用 **CloudBase API Key**（JWT 格式），写入前端代码，通过 `Authorization: Bearer {API_KEY}` 直接调用。

### 4.3 CodeBuddy 使用 CloudBase 成长计划 Token

**需求**：CodeBuddy 自带的 AI 额度耗尽后，想改用小程序成长计划的 1 亿 Token。

**配置方法**（在 CodeBuddy Code 中执行）：
```bash
codebuddy config set -g model "hy3-preview"
codebuddy config set -g endpoint "https://wordflash-d9gajqwvq157a1b06.api.tcloudbasegateway.com/v1/ai/hunyuan-v3"
codebuddy config set -g apiKeyHelper "printf '你的JWT_TOKEN'"
```

> 注意：当前 Cloud Studio 沙箱环境的 CodeBuddy 是托管版本，自定义 endpoint 可能被平台忽略，需新开会话验证是否生效。

---

## 五、词库数据类

### 5.1 各词库数据来源与格式

| 词库 | 来源文件 | 词量 | 格式 |
|------|----------|------|------|
| 四级 | COCA 排名 501-3500 | 2,613 | 已从 COCA 提取 |
| 六级 | `CET6_单词表.txt` | 2,219 | 已从 txt 导入 |
| 托福 | `wordlists/TOEFL.txt` | 4,510 | `word [phonetic] definition` |
| 雅思 | `wordlists/IELTS.txt` | 3,592 | `word [phonetic] pos. definition`（首行有 header） |
| 专八 | `wordlists/TEM8.txt` | 3,984 | `word [phonetic] definition`（同托福格式） |
| BEC | `wordlists/BEC.txt` | 2,347 | `word\tpos`（tab 分隔，无音标） |
| 高中 | COCA 排名 101-1500 | 1,249 | 已从 COCA 提取 |
| COCA 高频 10000 | `COCA_高频10000词.txt` | 8,670 | 含词频排名 |

### 5.2 词库数据加载方案演进

**方案 1（已废弃）**：后端解析 txt → 前端调用 `POST /api/data/init-libraries` 初始化
- 问题：小程序无法访问 localhost 后端，且依赖网络

**方案 2（当前方案）**：前端离线加载
1. 将词库数据预处理为 JSON 文件（`meta.json` + `lib-1.json` ~ `lib-8.json`）
2. 首次启动通过 `import()` 动态加载 JSON 文件
3. 解析后写入 `Taro.setStorageSync('wordflash_db', dbData)`
4. 后续启动直接读取 Storage，无需重新加载

**优点**：完全离线，不依赖后端，启动速度快（第二次以后）。

---

## 六、构建与部署类

### 6.1 `pnpm run build:weapp` 后 `project.config.json` 被覆盖

**问题**：每次执行 `build:weapp`，Taro 会重新生成 `weapp/project.config.json`，导致手动添加的 AppID 丢失。

**解决方案**：每次构建后手动重写 `weapp/project.config.json`，写入 AppID `wx91a54e09459c1f16`。

**长期方案**：在 `config/index.ts` 的 `weapp` 配置中设置 `appId`，让 Taro 构建时自动写入。

### 6.2 编译后代码是否适合二次开发

**结论**：
- `weapp/` 是编译产物，JS 变量名已被压缩，**不适合**直接修改业务逻辑
- 二次开发应基于 `frontend/` 源码，修改后重新执行 `pnpm run build:weapp`
- `weapp/` 只适合直接修改样式（`.wxss` 文件）

---

## 七、Git 与 GitHub 类

### 7.1 GitHub PAT 权限问题

**报错**：`Resource not accessible`（创建仓库）、`401 Bad credentials`（推送）

**原因**：第一个 Token 是 Fine-grained 类型，没有 `repo` 完整权限。

**解决方案**：重新生成 **Classic PAT**，勾选 `repo` 范围，通过：
```bash
git remote set-url origin https://用户名:TOKEN@github.com/用户名/仓库名.git
git push origin taro
```

### 7.2 taro 分支目录结构

taro 分支的代码放在 `wordflash-taro/` 子目录下，包含：
```
wordflash-taro/
├── frontend/        ← Taro 源码
├── weapp/           ← 编译后的微信小程序原生代码
├── wordlists/       ← 原始词库 txt 文件
└── WordFlash-weapp.zip  ← 打包好的原生代码压缩包
```

---

## 八、已解决的功能类问题

### 8.1 空词库点击"学习"显示"学习完成"

**原因**：词库为空时，闪卡逻辑返回 null，页面直接显示完成状态。

**修复**：
- 空词库不显示"学习"按钮，改为显示"点击 AI 生词添加"引导
- 防御性判断：词库为空时弹出提示"词库为空，请先添加单词"

### 8.2 进度条与加载体验

**实现**（`app.tsx` + `init-data.ts`）：
- 蓝紫渐变进度条：`linear-gradient(90deg, #3b82f6, #6366f1)`
- 进度百分比：5%（meta 加载完成）→ 10%~90%（各词库加载中）→ 92%（写入 Storage）→ 100%
- 提示文案："首次打开需要加载词库数据，稍等片刻就好~ 下次启动即可瞬间打开"

---

## 九、待解决 / 待办

- [ ] 配置 `config/index.ts` 的 `appId`，解决 `build:weapp` 后 AppID 被覆盖的问题
- [ ] 验证 CodeBuddy 自定义 endpoint 配置是否在新会话中生效
- [ ] 托福/雅思/专八/BEC 词库如使用新 txt 文件重新解析，需更新 `frontend/src/data/lib-3.json` ~ `lib-6.json`
- [ ] 微信小程序合法域名配置（`mp.weixin.qq.com`）
- [ ] 学习记录日历 / 连续打卡提醒
- [ ] 将 `frontend/` 源码的二次开发指南补充到 `README.md`（已完成）

---

*最后更新：2026-05-20*
