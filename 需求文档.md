# WordFlash 单词学习应用 - 需求文档

## 项目概述
基于 COCA 高频词库的英语单词学习应用，支持多词库管理、智能学习算法、进度追踪等功能。

---

## 一、词库管理需求

### 1.1 词库导入
- **支持格式**：每行一个单词，可选包含音标和释义
  - 标准格式：`abandon [əˈbændən] v. 抛弃，放弃`
  - 带排名格式：`the  #1  art. 这；那`
  - 纯单词格式：`abandon`
- **批量导入**：支持最多 10000 个单词批量导入
- **智能解析**：自动提取单词、音标、释义、词频排名
- **重复处理**：
  - 同一单词不同词频排名可共存（如 stall #6769 和 stall #6897）
  - 无词频的单词按拼写去重

### 1.2 词频排名系统
- **数据来源**：COCA (Corpus of Contemporary American English) 美国当代英语语料库
- **排名规则**：
  - 频率越高，排名数字越小（#1 = the，最高频）
  - 无词频的单词默认排名 99999（排在最后学习）
- **派生词继承**：无词频的派生词（如 vitally）自动从词根（vital）继承词频

### 1.3 词库统计
- 首页展示全局统计：
  - 总单词数（去重）
  - 已掌握数量
  - 学习中数量（快记住了 + 有点印象）
  - 未学习数量
  - 总体进度百分比（带进度条）

---

## 二、学习系统需求

### 2.1 学习算法
- **首次学习**：按词频排名顺序出题（频率高的先学）
- **复习阶段**：加权随机算法
  - 到期复习的单词权重 ×2.5
  - 掌握度越低权重越高
  - 未学过的单词权重 ×3（已移除，改为顺序学习）
- **掌握判定**：
  - 连续正确次数达标自动标记为已掌握
  - 用户可手动标记为已掌握

### 2.2 闪卡交互
- **正面**：显示单词、音标、词频排名（如果有）、发音按钮
- **背面**：显示释义、例句、词组
- **翻转动画**：点击卡片翻转查看释义
- **切换动画**：
  - 当前卡片向左滑出
  - 新卡片直接显示正面（无翻转动画）
  - 动画期间禁用 transition 避免闪烁

### 2.3 反馈选项
- 不认识（红色）
- 有点印象（蓝色）
- 快记住了（琥珀色）
- 已掌握（绿色）
- 直接标记已掌握（正面快捷按钮）

### 2.4 预取优化
- 学习当前卡片时，后台预取下一张卡片
- 预取时排除当前单词，避免重复
- 回退操作时清除预取缓存

---

## 三、数据导出需求

### 3.1 已掌握单词导出
- **触发方式**：点击首页"已掌握"统计数字
- **导出格式**：txt 文件，每行一个单词
- **文件名**：`已掌握单词_XX个.txt`
- **排序**：按词频升序排列

---

## 四、UI/UX 需求

### 4.1 首页布局
- 全局统计卡片（5 个指标）
- AI 智能生成词库入口
- 词库网格列表
- 词库卡片：图标、名称、单词数、操作按钮

### 4.2 学习页面
- 闪卡居中显示
- 底部反馈按钮栏
- 历史记录（可回退）
- 会话统计

### 4.3 词频排名展示
- 有词频的单词显示 `#排名` 标签
- 排名 99999 的单词不显示标签
- 卡片正反面都显示排名

---

## 五、数据模型需求

### 5.1 单词表 (words)
- id, word, phonetic, definitions, frequencyRank
- 联合唯一索引：(word, frequencyRank)

### 5.2 词库表 (wordLibraries)
- id, name, description, wordCount, category, isBuiltin

### 5.3 学习进度表 (wordProgress)
- id, userId, wordId, libraryId
- masteryScore, lastFeedback, reviewCount, isMastered
- lastReviewedAt, nextReviewAt

---

## 六、性能需求

### 6.1 加载优化
- 预取机制减少等待时间
- 卡片切换动画流畅（600ms）
- API 响应时间 < 200ms

### 6.2 数据处理
- 批量导入支持 10000 条数据
- 去重算法高效处理
- 数据库查询优化（索引、批量查询）
