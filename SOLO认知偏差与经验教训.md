# SOLO 认知偏差与经验教训

> 本文件记录 SOLO 在协作过程中的认知偏差、错误判断和经验教训。
>
> ⚠️ **使用规则（每次遇到问题必须先执行）：**
> 1. 遇到问题时，先查阅本文，搜索关键词看是否遇到过类似问题
> 2. 如果找到匹配记录，优先尝试已有解决方案
> 3. 如果已有方案不适用，尝试新的解决方法
> 4. 将新的解决方法追加到对应条目中（标注日期），**不要删除旧的行不通的方案**
> 5. 如果是全新的问题，新增一条记录

---

## 快速索引

| # | 关键词 | 简述 |
|---|--------|------|
| 1 | 文件访问、VM、Mac、挂载 | VM 里的文件就是用户本地文件 |
| 2 | AI 返回值、JSON 解析、防御性 | AI 返回格式不固定，需兼容多种 |
| 3 | callAI、callAILite、system prompt | 不同任务用不同 AI 方法 |
| 4 | 流程顺序、交互设计 | 先确认流程再动手 |
| 5 | 硬编码、AI 判断、映射表 | 用户要求 AI 判断时不要硬编码 |
| 6 | API Key、明文、加密、AES | 优先使用已有加密方案 |
| 7 | ngrok、IPv6、127.0.0.1 | ngrok 必须用 IPv4 地址 |
| 8 | Docker、Alpine、sqlite-dev | 原生模块需编译依赖 |

---

## 1. 文件访问认知错误

**日期：** 2026-04-24

**错误认知：**
> "我只能看到 VM 里的文件，VM 里的 vite.config.ts 已经有 allowedHosts: true。但我无法直接看到你 Mac 上的文件。"

**实际情况：**
用户通过 TRAE 选择的文件夹会挂载到 VM 中，VM 里看到的文件就是用户本地的文件，是同一个文件。不存在"VM 里的版本"和"Mac 上的版本"不一致的问题（除非用户在 Mac 上用其他编辑器同时修改了文件）。

**正确做法：**
- VM 里读到的文件就是用户的本地文件，直接修改即可
- 如果用户反馈问题仍然存在，应考虑其他原因（如没重启服务、浏览器缓存等），而不是怀疑文件不同步

---

## 2. AI 返回值格式兼容性不足

**日期：** 2026-04-24

**错误认知：**
AI 返回的 JSON 数组格式是 `[9]`（数字），直接用 `typeof item === "number"` 判断。

**实际情况：**
AI 实际返回了 `["9"]`（字符串）和 `[{"id":9}]`（对象）等多种格式，导致解析失败。

**正确做法：**
- 对 AI 返回值做防御性解析，兼容数字、字符串、对象三种格式
- 不要假设 AI 会严格按照 prompt 要求的格式返回

---

## 3. 复用错误的 AI 调用方法

**日期：** 2026-04-24

**错误认知：**
词库匹配功能直接复用了 `callAI`（词汇生成专用的方法）。

**实际情况：**
`callAI` 的 system prompt 是"英语词汇专家"，要求返回 `{...}` JSON，而词库匹配需要返回 `[...]` 数组，两者冲突。

**正确做法：**
- 不同任务用独立的 AI 调用方法，各自有独立的 system prompt
- 新增了 `callAILite` 通用方法，支持自定义 system prompt

---

## 4. 流程理解反了

**日期：** 2026-04-24

**错误认知：**
用户要求"先选排除词库再生成单词"，SOLO 理解成了"先生成单词再匹配相关词库"。

**正确做法：**
- 涉及多步骤交互的需求，先复述完整流程让用户确认
- 不要凭直觉假设流程顺序

---

## 5. 硬编码映射 vs AI 判断

**日期：** 2026-04-24

**错误认知：**
用户说"匹配相关词库"，SOLO 用硬编码的中英文映射表（天气→weather）来实现。

**实际情况：**
用户明确要求用 AI 判断匹配，不要硬编码。

**正确做法：**
- 用户对技术方案有明确偏好时，直接按用户要求做
- 如果不确定，先问清楚再动手

---

## 6. API Key 明文存储

**日期：** 2026-04-24

**错误认知：**
在 .env 文件中写了明文 API Key。

**实际情况：**
之前已经实现了 AES-256-GCM 加密存储方案，不应该再用明文。

**正确做法：**
- 记住已有的技术决策和实现，不要重复犯错
- 涉及安全的配置，优先使用已有的加密方案

---

## 7. ngrok 启动命令

**日期：** 2026-04-24

**错误认知：**
直接用 `ngrok http 3000` 启动。

**实际情况：**
ngrok 默认尝试 IPv6 地址 `[::1]:3000`，而项目监听的是 IPv4 `0.0.0.0`，导致连接被拒绝。

**正确做法：**
- 用 `ngrok http 127.0.0.1:3000` 明确指定 IPv4 地址

---

## 8. Docker Alpine 缺少 sqlite-dev

**日期：** 2026-04-24

**错误认知：**
Dockerfile 只装了 `python3 make g++`，没装 `sqlite-dev`。

**实际情况：**
`better-sqlite3` 是原生模块，需要 sqlite 开发头文件才能编译。

**正确做法：**
- 使用原生 Node 模块（如 better-sqlite3、esbuild）时，确保 Docker 镜像有对应的编译依赖
- Alpine Linux 常见遗漏：`sqlite-dev`、`vips-dev`、`cairo-dev` 等
